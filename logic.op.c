#include <stdio.h>

int main(int argc, char *argv[])
{
    /*
    Logic Operators

    && -> AND (conjunction)
    || -> OR (disjunction)
    ! -> (negation)
    
    */
    
    int n1 =10, n2 =20; 
    printf("%d", (n1 < 20) && (n2 >= 20));

    return 0;   
}